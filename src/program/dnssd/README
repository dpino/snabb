Usage: snabb dnssd OPTS <iface>

Options:

   -p|--pcap <filename>    Read incoming packets from pcap file.

Lists all devices and services available in a local network.  Press Ctrl+C to stop program.

Example:

$ sudo ./snabb dnssd wlp3s0
